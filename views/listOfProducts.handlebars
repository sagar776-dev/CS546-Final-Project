<article class="product-list">
    <ul>
        {{#each categoryList}}
        <li>
            <a href="products/{{this}}">{{this}}</a>
        </li>
        {{/each}}
        {{#each manufactortList}}
        <li>
            <a href="products/{{this}}">{{this}}</a>
        </li>
        {{/each}}
    </ul>
    <table>
        {{#each productList}}
        <tr>
            <td>
                <img src={{ pictures.[0] }} alt={{pictures.[0]}} width="100" height="100">
            </td>
            <td>
                <a href="products/{{category}}/{{_id}}">{{_id}}</a>
                {{name}}
            </td>
        </tr>
        {{/each}}
    </table>
</article>